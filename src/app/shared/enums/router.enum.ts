/**
 * Main Routes Enum
 */

export enum ROUTES {
  LANDING_PAGE = 'welcome',
  FIND_RIDE = 'find-ride',
  LOGIN_PAGE = 'login',
  REGISTER_PAGE = 'register',
  AUTH_CALLBACK = 'callback',
  RIDE = 'rides',
  POST = 'post',
  AUTH = 'auth',
  SEARCH = 'search'
}
