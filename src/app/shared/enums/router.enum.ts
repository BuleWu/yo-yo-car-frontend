/**
 * Main Routes Enum
 */

export enum ROUTES {
  LANDING_PAGE = 'welcome',
  FIND_RIDE = 'find-ride',
  LOGIN_PAGE = 'login',
  REGISTER_PAGE = 'register',
  AUTH_CALLBACK = 'callback',
  RIDES = 'rides',
  RIDE = 'ride',
  POST = 'post',
  AUTH = 'auth',
  SEARCH = 'search',
  EDIT = 'edit',
  USER = 'user',
  RATINGS = 'ratings',
  CHATS = 'chats',
  USERS = 'users',
  LEAVE_RATING = 'leave-rating'
}
